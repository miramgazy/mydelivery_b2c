# 🔍 Диагностика проблем со сборкой Frontend

## Текущая ситуация

Frontend контейнер еще развертывается. Возможные причины задержки:

## Возможные проблемы

### 1. Сборка занимает много времени
- На маломощном VPS (2 CPU) сборка Vite может занимать 5-10 минут
- Особенно при первой сборке (установка node_modules)

### 2. Недостаточно памяти
- Сборка frontend требует ~1-2GB RAM
- Если памяти не хватает, сборка может падать

### 3. Отсутствуют переменные окружения
- `VITE_API_URL`
- `VITE_TELEGRAM_BOT_USERNAME`
- `VITE_DEV_MODE`

### 4. Ошибки в процессе сборки
- Проблемы с зависимостями npm
- Ошибки компиляции Vue/Vite

## Проверка

### 1. Проверка логов сборки в Coolify
В интерфейсе Coolify проверьте логи сборки frontend контейнера.

### 2. Проверка статуса контейнера
```bash
docker ps | grep frontend | grep w88c4ogc88gk00w8sog08skk
```

### 3. Проверка логов frontend контейнера
```bash
docker logs frontend-w88c4ogc88gk00w8sog08skk-XXXXX
```

### 4. Проверка использования ресурсов
```bash
docker stats | grep frontend
```

## Решения

### Если сборка слишком долгая
1. Подождите еще 5-10 минут
2. Проверьте логи на наличие ошибок
3. Убедитесь, что в Coolify указаны переменные:
   - `VITE_API_URL=/api`
   - `VITE_TELEGRAM_BOT_USERNAME=TG_MiniAppTest_Bot`
   - `VITE_DEV_MODE=false`

### Если не хватает памяти
1. Увеличьте лимит памяти для frontend в docker-compose.coolify.yml
2. Или временно остановите другие контейнеры

### Если есть ошибки сборки
1. Проверьте логи на наличие конкретных ошибок
2. Убедитесь, что все файлы frontend присутствуют
3. Проверьте package.json на корректность зависимостей

## Ожидаемое время сборки

- **Первая сборка**: 5-10 минут (установка node_modules + сборка)
- **Последующие сборки**: 2-5 минут (если кэш работает)

## После успешной сборки

Frontend должен:
1. Собраться в production образ с nginx
2. Запуститься на порту 80 внутри контейнера
3. Быть доступным через nginx контейнер проекта

## Проверка после запуска

```bash
# Проверка frontend контейнера
docker logs frontend-w88c4ogc88gk00w8sog08skk-XXXXX

# Проверка доступности
curl http://localhost:XXXX/  # Замените XXXX на порт nginx контейнера
```
