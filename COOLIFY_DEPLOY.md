# 🚀 Деплой в Coolify

## Оптимизации для предотвращения зависания сервера

### Что было исправлено:

1. **Последовательный запуск сервисов**
   - Все зависимости используют `condition: service_healthy`
   - Backend ждет готовности Redis
   - Celery и Celery-Beat ждут готовности Backend и Redis
   - Frontend ждет готовности Backend
   - Nginx ждет готовности Backend и Frontend

2. **Healthchecks**
   - Backend проверяется через `curl http://localhost:8000/api/`
   - Redis проверяется через `redis-cli ping`
   - Start period для backend увеличен до 60 секунд (время на миграции)

3. **Разделение миграций и запуска сервера**
   - Создан специальный `docker-entrypoint-coolify.sh`
   - Миграции выполняются ДО запуска gunicorn
   - Это предотвращает зависание при параллельном запуске

4. **Ограничения ресурсов**
   - Backend: максимум 2 CPU, 2GB RAM
   - Celery: максимум 1 CPU, 1GB RAM
   - Celery-Beat: максимум 0.5 CPU, 512MB RAM
   - Frontend: максимум 1 CPU, 1GB RAM
   - Nginx: максимум 0.5 CPU, 256MB RAM
   - Redis: максимум 0.5 CPU, 512MB RAM (с политикой очистки памяти)

5. **Оптимизация Redis**
   - Добавлена политика `allkeys-lru` для автоматической очистки памяти
   - Ограничение памяти до 256MB

## Сервисы после деплоя:

| URL | Назначение |
|-----|------------|
| `/` | TMA (Telegram Mini App) |
| `/webapp/?org=<uuid>` | Веб-сайт доставки (каталог по организации) |
| `/administrator/` | Django Admin |
| `/api/` | REST API |

## Последовательность запуска:

```
1. Redis (с healthcheck)
   ↓
2. Backend (ждет Redis, выполняет миграции, запускает gunicorn)
   ↓
3. Celery Worker (ждет Backend и Redis)
   ↓
4. Celery Beat (ждет Backend и Redis)
   ↓
5. Frontend (ждет Backend)
   ↓
6. Front-website (ждет Backend)
   ↓
7. Nginx (ждет Backend, Frontend, Front-website)
```


## Переменные окружения в Coolify:

| Переменная | Обязательно | Описание |
|------------|-------------|----------|
| `DATABASE_URL` | Да | URL PostgreSQL (Coolify может подставлять автоматически) |
| `SECRET_KEY` | Да | Секретный ключ Django (сгенерируйте надёжный) |
| `DEBUG` | Да | `0` для production |
| `ALLOWED_HOSTS` | Да | Домен через запятую, напр. `yourdomain.com,www.yourdomain.com` |
| `IIKO_API_BASE_URL` | Да | `https://api-ru.iiko.services/api/1` |
| `CORS_ALLOWED_ORIGINS` | Да | Разрешённые источники, напр. `https://yourdomain.com,https://www.yourdomain.com` |
| `CSRF_TRUSTED_ORIGINS` | Да | Доверенные домены для CSRF, напр. `https://yourdomain.com,https://www.yourdomain.com` |
| `CELERY_BROKER_URL` | Нет | По умолчанию `redis://tg-redis:6379/0` |
| `CELERY_RESULT_BACKEND` | Нет | По умолчанию `redis://tg-redis:6379/0` |
| `SECURE_SSL_REDIRECT` | Нет | `1` если за Cloudflare/SSL |
| `SESSION_COOKIE_SECURE` | Нет | `1` для HTTPS |
| `CSRF_COOKIE_SECURE` | Нет | `1` для HTTPS |

**Примечание:** Токены ботов (`bot_token`, `bot_username`) задаются в админке Django для каждой организации.

## Использование:

1. В Coolify выберите `docker-compose.coolify.yml` как файл конфигурации
2. Убедитесь, что все переменные окружения настроены
3. Запустите деплой

## Troubleshooting:

### Если сервер все еще зависает:

1. **Проверьте логи backend:**
   ```bash
   docker logs <backend-container-name>
   ```

2. **Увеличьте start_period для healthcheck:**
   В `docker-compose.coolify.yml` увеличьте `start_period: 60s` до `start_period: 120s`

3. **Проверьте ресурсы сервера:**
   Убедитесь, что на сервере достаточно ресурсов для всех контейнеров

4. **Проверьте подключение к БД:**
   Убедитесь, что `DATABASE_URL` правильно настроен в Coolify

### Если миграции не выполняются:

Entrypoint скрипт автоматически выполняет миграции перед запуском сервера. Если миграции не выполняются:

1. Проверьте логи entrypoint
2. Убедитесь, что `DATABASE_URL` доступен
3. Проверьте права доступа к БД
