# 🔐 Доступ к Django Admin панели

## URL админки

После настройки внешнего nginx, админка доступна по адресу:

**https://b2c-delivery.mevent.kz/admin/**

## Требования

1. ✅ Суперпользователь должен быть создан (см. `CREATE_SUPERUSER.md`)
2. ✅ Внешний nginx должен быть настроен и проксировать на порт 3090
3. ✅ Все контейнеры должны быть запущены и здоровы

## Вход в админку

### Шаг 1: Откройте админку в браузере

Перейдите по адресу:
```
https://b2c-delivery.mevent.kz/admin/
```

### Шаг 2: Введите учетные данные

Используйте данные суперпользователя, созданного ранее:
- **Username** (имя пользователя)
- **Password** (пароль)

### Шаг 3: Войдите

После успешного входа вы увидите Django Admin панель с доступными моделями.

## Что доступно в админке

В зависимости от настроенных моделей, вы сможете управлять:

- **Users** - пользователи системы
- **Organizations** - организации
- **Terminals** - терминалы
- **Products** - продукты/товары
- **Orders** - заказы
- **Cities** - города
- **Streets** - улицы
- **Delivery Addresses** - адреса доставки
- И другие модели, зарегистрированные в `admin.py`

## Проверка доступности

### Проверка через curl

```bash
# Проверка доступности админки
curl -I https://b2c-delivery.mevent.kz/admin/

# Должен вернуть HTTP 302 (редирект на страницу входа) или 200
```

### Проверка через браузер

1. Откройте `https://b2c-delivery.mevent.kz/admin/`
2. Должна открыться страница входа Django Admin
3. Если видите ошибку - проверьте настройки ниже

## Устранение проблем

### Проблема: "404 Not Found"

**Причина:** Nginx не проксирует запросы на `/admin/`

**Решение:**
1. Проверьте конфигурацию внешнего nginx
2. Убедитесь, что `location /` проксирует на порт 3090
3. Проверьте, что внутренний nginx контейнера проксирует `/admin/` на backend

### Проблема: "502 Bad Gateway"

**Причина:** Backend контейнер не запущен или недоступен

**Решение:**
```bash
# Проверьте статус контейнеров
docker ps | grep backend

# Проверьте логи backend
docker logs backend-w88c4ogc88gk00w8sog08skk-134042200204
```

### Проблема: "403 Forbidden" или "CSRF verification failed"

**Причина:** Проблемы с CSRF токенами или SSL

**Решение:**
1. Убедитесь, что используете HTTPS
2. Проверьте настройки `CSRF_COOKIE_SECURE` и `SESSION_COOKIE_SECURE` в Django settings
3. Убедитесь, что `X-Forwarded-Proto` заголовок передается правильно

### Проблема: Статические файлы не загружаются (CSS/JS отсутствуют)

**Причина:** Статические файлы не собраны или не доступны

**Решение:**
```bash
# Проверьте, что статические файлы собраны
docker exec -it backend-w88c4ogc88gk00w8sog08skk-134042200204 python manage.py collectstatic --noinput

# Проверьте, что volume django_static подключен
docker volume ls | grep django-static
```

### Проблема: Страница входа не отображается корректно

**Причина:** Проблемы с проксированием или заголовками

**Решение:**
1. Проверьте конфигурацию nginx на наличие правильных заголовков:
   ```nginx
   proxy_set_header Host $host;
   proxy_set_header X-Real-IP $remote_addr;
   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
   proxy_set_header X-Forwarded-Proto $scheme;
   ```

## Альтернативный доступ (для отладки)

Если внешний nginx еще не настроен, можно получить прямой доступ к backend:

### Через порт контейнера (если открыт)

```bash
# Найдите порт backend контейнера
docker ps | grep backend

# Если порт открыт, можно использовать:
# http://your-server-ip:PORT/admin/
```

### Через SSH туннель

```bash
# Создайте SSH туннель
ssh -L 8000:localhost:8000 user@your-server

# Затем откройте в браузере:
# http://localhost:8000/admin/
```

## Безопасность

⚠️ **Важно для production:**

1. Используйте сильные пароли для суперпользователей
2. Ограничьте доступ к админке по IP (через nginx или firewall)
3. Используйте HTTPS (SSL сертификаты)
4. Регулярно обновляйте Django и зависимости
5. Рассмотрите использование двухфакторной аутентификации

## Дополнительные ссылки

- Создание суперпользователя: `CREATE_SUPERUSER.md`
- Настройка nginx: `NGINX_QUICK_FIX.md`
- API документация: `https://b2c-delivery.mevent.kz/api/schema/swagger-ui/`
