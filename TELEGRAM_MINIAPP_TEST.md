# 🧪 Тестирование Telegram Mini App - Onboarding

## Что нужно проверить:

### 1. Приветствие (WelcomeView)
**URL:** `/onboarding/welcome`

**Проверка:**
- [ ] Отображается приветственное сообщение "Привет! 👋"
- [ ] Показывается название организации (если доступно)
- [ ] Кнопка "Продолжить" работает
- [ ] При клике на "Продолжить" происходит переход на `/onboarding/phone`

**Когда показывается:**
- Пользователь авторизован в Telegram
- У пользователя нет телефона (`!user?.phone`)

**Код проверки:**
```javascript
// В App.vue и router/index.js
if (!user?.phone) {
    router.push('/onboarding/welcome')
}
```

---

### 2. Запрос номера телефона (PhoneInputView)
**URL:** `/onboarding/phone`

**Проверка:**

#### В Telegram Mini App:
- [ ] Отображается кнопка "Поделиться номером телефона" (только в Telegram)
- [ ] При клике на кнопку открывается диалог Telegram для отправки контакта
- [ ] После отправки контакта номер автоматически подтягивается с бекенда
- [ ] Номер автоматически форматируется (например: `+77001234567`)
- [ ] После получения номера происходит автоматический переход на `/onboarding/address`
- [ ] Если пользователь отменил отправку - показывается поле для ручного ввода
- [ ] Если номер не получен за 5 секунд - показывается поле для ручного ввода

#### Ручной ввод:
- [ ] Поле для ввода телефона отображается
- [ ] Форматирование телефона работает (автоматически добавляется +7)
- [ ] Валидация формата телефона работает (11 цифр, начинается с 7)
- [ ] Кнопка "Продолжить" активна только при заполненном телефоне
- [ ] При сохранении показывается индикатор загрузки
- [ ] После сохранения происходит переход на `/onboarding/address`

**API вызов:**
```javascript
await usersService.updateProfile({ phone: phone.value })
await authStore.fetchCurrentUser()
router.push('/onboarding/address')
```

**Обработка ошибок:**
- [ ] При ошибке сохранения показывается сообщение об ошибке
- [ ] Пользователь может повторить попытку

---

### 3. Запрос адреса доставки (AddressInputView)
**URL:** `/onboarding/address`

**Проверка:**
- [ ] Отображается форма с полями:
  - [ ] Город (обязательное)
  - [ ] Улица (обязательное)
  - [ ] Дом (обязательное)
  - [ ] Квартира/Офис (необязательное)
  - [ ] Комментарий (необязательное)
- [ ] Показывается название организации (автоматически из контекста бота)
- [ ] Кнопка "Пропустить" работает (переход на `/onboarding/terminal`)
- [ ] Кнопка "Сохранить" работает
- [ ] Валидация обязательных полей работает
- [ ] При сохранении показывается индикатор загрузки
- [ ] После сохранения происходит переход на `/onboarding/terminal`

**API вызов:**
```javascript
await deliveryAddressService.createAddress({
    city_name: form.city_name,
    street_name: form.street_name,
    house: form.house,
    flat: form.flat,
    comment: form.comment,
    is_default: true
})
await authStore.fetchCurrentUser()
router.push('/onboarding/terminal')
```

**Обработка ошибок:**
- [ ] Если организация не определена - показывается ошибка
- [ ] При ошибке сохранения показывается сообщение об ошибке

---

## Логика роутинга (router/index.js)

### Проверка последовательности onboarding:

1. **Welcome** → показывается если `!user?.phone`
2. **Phone** → показывается после Welcome или если `!user?.phone`
3. **Address** → показывается если `user?.phone && !user?.addresses`
4. **Terminal** → показывается если `user?.phone && user?.addresses.length > 0 && !user?.terminals`

**Проверка:**
- [ ] Если пользователь без телефона пытается зайти на главную - редирект на `/onboarding/welcome`
- [ ] Если пользователь с телефоном, но без адреса - редирект на `/onboarding/address`
- [ ] Если пользователь с телефоном и адресом, но без терминалов - редирект на `/onboarding/terminal`
- [ ] Если все данные заполнены - доступ к основному функционалу

---

## Тестирование в реальном Telegram Mini App

### Подготовка:
1. Убедитесь, что бот настроен и Mini App URL указан правильно
2. Откройте Mini App через бота в Telegram
3. Используйте тестовый аккаунт Telegram

### Сценарий 1: Новый пользователь
1. Откройте Mini App впервые
2. Должен показаться экран Welcome
3. Нажмите "Продолжить"
4. Должен показаться экран Phone
5. Нажмите "Поделиться номером телефона"
6. Подтвердите отправку контакта
7. Должен автоматически перейти на экран Address
8. Заполните адрес и нажмите "Сохранить"
9. Должен перейти на экран Terminal

### Сценарий 2: Пользователь отменил отправку контакта
1. На экране Phone нажмите "Поделиться номером телефона"
2. Отмените отправку контакта
3. Должно появиться поле для ручного ввода
4. Введите номер вручную
5. Нажмите "Продолжить"
6. Должен перейти на экран Address

### Сценарий 3: Пользователь пропустил адрес
1. На экране Address нажмите "Пропустить"
2. Должен перейти на экран Terminal

---

## Проверка в консоли браузера

Откройте DevTools (F12) и проверьте:

1. **Инициализация Telegram:**
```javascript
console.log('TG User:', tgUser.id)
```

2. **Данные пользователя после логина:**
```javascript
console.log('User after login:', user)
console.log('User phone:', user?.phone)
console.log('User addresses:', user?.addresses)
```

3. **Редиректы:**
```javascript
console.log('User has no phone, redirecting to welcome')
console.log('User has no address, redirecting to address input')
```

4. **API запросы:**
- Проверьте Network tab на наличие запросов:
  - `POST /api/users/profile/` (обновление телефона)
  - `POST /api/delivery-addresses/` (создание адреса)
  - `GET /api/users/me/` (получение данных пользователя)

---

## Возможные проблемы и решения

### Проблема: Welcome не показывается
**Причина:** Пользователь уже имеет телефон в базе
**Решение:** Удалите телефон пользователя через админку или API

### Проблема: Номер не подтягивается автоматически
**Причина:** Бот не обработал контакт или API не вернул телефон
**Решение:** 
- Проверьте логи бота
- Проверьте API `/api/users/me/` - должен вернуть `phone`
- Проверьте, что бот обрабатывает событие `contact`

### Проблема: Редирект зацикливается
**Причина:** Данные пользователя не обновляются после сохранения
**Решение:** Убедитесь, что вызывается `await authStore.fetchCurrentUser()` после каждого сохранения

### Проблема: Адрес не сохраняется
**Причина:** Пользователь не привязан к организации
**Решение:** Убедитесь, что пользователь имеет `organization` в данных

---

## Команды для проверки через API

### Проверить данные пользователя:
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  https://b2c-delivery.mevent.kz/api/users/me/
```

### Очистить телефон пользователя (для тестирования):
```bash
curl -X PATCH \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"phone": ""}' \
  https://b2c-delivery.mevent.kz/api/users/profile/
```

### Удалить адреса пользователя:
```bash
# Получить список адресов
curl -H "Authorization: Bearer YOUR_TOKEN" \
  https://b2c-delivery.mevent.kz/api/delivery-addresses/

# Удалить адрес (замените ID)
curl -X DELETE \
  -H "Authorization: Bearer YOUR_TOKEN" \
  https://b2c-delivery.mevent.kz/api/delivery-addresses/{id}/
```
