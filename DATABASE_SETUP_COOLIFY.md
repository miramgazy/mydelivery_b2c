# 🗄️ Настройка базы данных для Coolify

## Проблема
В `docker-compose.coolify.yml` отсутствовал сервис базы данных PostgreSQL.

## Решение
Добавлен сервис `db` с PostgreSQL 15-alpine.

## Конфигурация базы данных

### Вариант 1: Локальная БД (внутри контейнера)
Используется, если в .env файле Coolify НЕ указан `DATABASE_URL` или указаны отдельные переменные:
- `DB_HOST=db`
- `DB_PORT=5432`
- `DB_NAME=mydelivery_b2c`
- `DB_USER=postgres`
- `DB_PASSWORD=postgres`

### Вариант 2: Внешняя БД (управляется Coolify)
Используется, если в .env файле Coolify указан `DATABASE_URL` от Coolify.

Entrypoint скрипт автоматически определяет, какой вариант использовать.

## Переменные окружения

### Обязательные для локальной БД:
```env
DB_NAME=mydelivery_b2c
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=db
DB_PORT=5432
```

### Или используйте DATABASE_URL от Coolify:
```env
DATABASE_URL=postgresql://user:password@host:port/database
```

## Последовательность запуска

```
1. db (PostgreSQL) - запускается первым
   ↓
2. tg-redis (Redis) - запускается параллельно с БД
   ↓
3. backend - ждет готовности db и tg-redis
   ↓
4. celery, celery-beat - ждут готовности db, tg-redis и backend
   ↓
5. frontend - ждет готовности backend
   ↓
6. nginx - ждет готовности backend и frontend
```

## Ресурсы БД

- **CPU**: 0.5 (лимит), 0.1 (резерв)
- **Memory**: 512MB (лимит), 128MB (резерв)
- **Port**: 5432 (внутренний), настраивается через `DB_PORT`

## Проверка

```bash
# Проверка статуса БД контейнера
docker ps | grep db

# Проверка логов БД
docker logs <db-container-name>

# Проверка подключения
docker exec <db-container-name> pg_isready -U postgres

# Подключение к БД
docker exec -it <db-container-name> psql -U postgres -d mydelivery_b2c
```

## Важно

1. Если используете внешнюю БД от Coolify, убедитесь, что `DATABASE_URL` правильно настроен
2. Если используете локальную БД, убедитесь, что все переменные `DB_*` указаны в .env
3. БД контейнер должен быть в сети `coolify` для доступа из других контейнеров Coolify (если нужно)
